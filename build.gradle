plugins {
    id 'play'
}

repositories {
    mavenCentral()
    maven { url 'http://repo.typesafe.com/typesafe/releases/' }
}

model {
    components {
        play {
            platform play: '2.3.9', java: '1.8', scala: '2.11'
        }
    }
}

